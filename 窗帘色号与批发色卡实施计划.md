# 窗帘固定色号、公开色卡与批发销售单实施计划

日期：2026-09-13
状态：源码已按计划实现，待在目标 Bench 环境迁移、整理商品资料并完成验收。
依据：《ERPNext 定制开发操作手册.md》（2026-09-13 更新）与本次需求讨论。

## 1. 目标与确认范围

同款窗帘使用同一个原包装条码，供应商不提供颜色编号，也不承担分色标签工作。Solua Home 在首次到货时建立固定色号及色卡，让采购入库、库存、零售、批发单据和客户再次订货使用一致的颜色标识。

| 已确认需求 | 结果 |
|---|---|
| 客人对颜色名称理解不同 | 使用款号加数字尾号作为明确的订货标识，颜色名辅助辨认 |
| 原包装同款共用条码 | 保留原条码，扫描定位款式，再选择颜色 |
| 建立实物对应 | 首批按实物分色、编号、拍照，并保留布样或参考样 |
| 减少日常工作量 | 不要求每条商品或每个分装包重新贴标签；通过色卡和单据辨认，客人可自行打印标签 |
| 对外查色卡 | 使用现有 erp.solua.one，无需登录，按货号查看颜色图片 |
| 内部模板库存 | 展示各颜色库存及整款合计，可按仓库查看 |
| 库存公开范围 | 仅内部查看，公开页面及公开查询响应都不提供库存 |
| 批发销售单 | 按具体颜色分行，显示完整订货货号、色号及销售数量 |
| 单据图片与二维码 | 两个独立开关，每次打印按客人需要选择 |

本次实施已完成自定义 App 的源码改动；尚未执行生产迁移、现有商品编号映射、库存变更、服务器部署或安装包构建。

当前已落地：Item 固定色号/对外货号/公开开关字段；固定色号由商品负责人根据供应商实物色卡手动选择，系统只校验数字格式、对外货号唯一性，并在填写色号后组合对外订货货号；公开 `/colors` 色卡页面和安全查询接口；登录用户可见的模板颜色库存明细与合计；Sales Invoice 批发颜色版打印格式，以及图片、二维码两个独立打印开关。二维码生成依赖已加入自定义 App 的 `requirements.txt`。

## 2. 编号与商品资料规则

### 2.1 建议采用的编号形式

使用“款式货号-两位色号”，例如 SH151015-01、SH151015-02。数字只在当前款式内有意义，不建立全店通用的“01 等于白色”规则。

| 款式货号 | 完整订货货号 | 固定色号 | 辅助颜色名 |
|---|---|---|---|
| SH151015 | SH151015-01 | 01 | 米白 |
| SH151015 | SH151015-02 | 02 | 浅灰 |
| SH151039 | SH151039-01 | 01 | 深灰 |

以上仅为示例，不是已核实的商品颜色或实际库存。

规则：

1. 按实物确认编号，不按颜色名称字母顺序自动重排。
2. 使用过的编号不改、不重新分配给另一颜色；新颜色的编号由商品负责人根据供应商实物色卡和已建立的参考样手动选择，系统不按创建顺序自动分配。
3. 颜色名称可以修正，稳定的完整订货货号保持不变。
4. 不按图片相似度自动合并颜色；无法判断时由负责商品的人对照实物样决定。
5. 轻微批次色差与新增颜色的分界由商品负责人确认。建议轻微色差保留编号、拣货尽量同批；明显不同且不能混卖时新建色号。
6. 本期不默认引入 ERP 批次追踪模块。若以后需要严格追踪染色批次，另列需求。

### 2.2 现有 SKU 的衔接

手册中同时存在颜色缩写 SKU 和后续窗帘主数据导入记录，不能据此认定所有现有商品都已是模板或都有颜色库存。实施前必须逐款核实。

| 现有情况 | 处理方向 |
|---|---|
| 新建款式、无历史单据 | 新建模板，具体变体优先直接采用数字尾号货号 |
| 已有颜色变体及历史交易 | 优先保留内部 item_code，增加唯一的对外订货货号与固定色号映射 |
| 现有普通库存物料，需要拆成颜色变体 | 单独制定库存及单据衔接方案，不直接勾选模板、不把旧库存复制给各颜色 |
| 已有库存但不清楚颜色数量 | 先实物盘点，取得分色数量后才能迁移 |

保留内部编码时，搜索、开单、色卡、打印必须把对外货号准确解析到唯一 SKU，业务单据仍引用真实 Item。不能出现同一订货货号对应两个 SKU。

选择映射还是重命名是实施核查后的技术决策；不得在核查前批量改名。历史单据不应因为重新编号而失去可追溯关系。

### 2.3 每个颜色需维护的资料

| 资料 | 要求 |
|---|---|
| 款式关联 | 指向唯一模板 |
| 固定色号 | 本款内唯一，保存为文本以保留 01 的前导零 |
| 完整订货货号 | 全局唯一，用于客户搜索、订货和单据展示 |
| 颜色名称 | 辅助描述；中文先完善，葡语字段按需要补充 |
| 色卡图片 | 一张能辨认颜色和面料的实拍图，可点击放大 |
| 实物样编号 | 与完整订货货号一致，便于后续到货归类 |
| 公开状态 | 明确决定是否对外发布，未整理完成的颜色不自动公开 |
| 停用状态 | 不再销售的颜色保留编号；已公开的旧色号可显示停用提示 |

优先复用 Item、variant_of、Cor 属性及 custom_swatch_image，新增字段只用于缺失信息。Cor 的颜色名不再承担稳定身份标识职责。色号字段和具体字段名在核查现有配置后确定。

## 3. 日常操作流程

### 3.1 首次建色卡

1. 按款式整理到货，将同款按实物颜色分组。
2. 商品负责人根据供应商实际提供的色卡和实物参考样，手动选择并填写 01、02 等固定色号。
3. 在尽量一致的光线和背景下拍照，画面应能看清颜色、纹理；核对图片与编号对应关系。
4. 留布样或参考样，写明完整订货货号。
5. 在 ERP 建立或关联变体，填写色号、辅助名称和色卡图片。
6. 核对成本、销售价、单位及默认仓库；沿用已有含税定价规则，不在本项目重设价格体系。
7. 预览色卡页面，确认后发布。
8. 按实际业务使用采购收货或期初库存流程，将分色数量记到具体 SKU。

### 3.2 补货与新增颜色

1. 查找对应款式及现有色卡。
2. 以实物参考样确认颜色，图片辅助查找。
3. 已有颜色沿用原 SKU；新颜色分配新编号、补图并建变体。
4. 分色录入收货数量，不将模板作为库存交易商品。
5. 需要同房间配色的订单，拣货时检查实物一致性。

### 3.3 批发开单与交货

1. 输入款式货号或扫描共用条码。
2. 查看带固定色号的图片选择器，选择颜色并逐色输入数量。
3. 保存时每行使用具体 SKU，不能仅在备注中写颜色而仍扣同一个普通物料的库存。
4. 根据客人需求选择打印图片、打印二维码。
5. 员工按颜色行拣货，客人根据单据或公开色卡对照实物。
6. 不普遍增加分装贴标；仅难辨认颜色可使用编号纸或交货现场核对。
7. 客人可自行打印标签，后续按完整订货货号补货。本期不默认建设客户在线标签打印系统。

## 4. 对外色卡查询页面

### 4.1 入口与查询行为

沿用 erp.solua.one。入口为 `/colors`，查询链接使用 `/colors?q=SH151015`（二维码也使用该稳定查询地址）；上线前检查站点路由是否冲突。

| 输入或操作 | 预期行为 |
|---|---|
| 款式货号 | 显示本款所有已公开颜色 |
| 完整订货货号 | 打开所属款式并定位对应颜色 |
| 原包装共用条码 | 定位对应款式；作为已有扫码逻辑的便捷入口 |
| 销售单二维码 | 直接打开对应款式色卡，不要求登录 |
| 点击图片 | 放大查看 |
| 无匹配或未公开商品 | 给出简单的未找到提示，不泄露内部资料 |
| 已公开后停用的颜色 | 保留识别信息并标记停用，不能把原编号替换为新色 |

货号查询优先精确匹配，忽略首尾空格；模糊搜索、多条件筛选和整站商品目录不作为第一期必要范围。

### 4.2 页面展示范围

公开：款式名称、款式货号、完整订货货号、色号、颜色名、颜色图片、规格、销售单位及停用提示。

不公开：库存与库存状态、仓库、成本、采购价、客户资料、内部备注、同步凭据。价格暂不纳入色卡页。

公开接口采用明确字段清单，只返回已发布资料；不通过“接口返回后前端隐藏”实现库存保密。图片需有经过选择的公开访问方式，不能把内部附件目录整体开放。

页面适配手机，无登录要求。界面文字纳入翻译；中文先交付，葡语按钮和颜色数据的完善另行核对，不把缺少翻译当成已经完成。

### 4.3 图片与链接维护

优先使用每个变体的 custom_swatch_image。缺少真实色卡图时显示“图片待补”，不拿同款通用图冒充该色图片；未完成资料可保留为未发布。

链接以稳定款式标识生成，已印二维码不因普通改名或图片更新失效。图片更新后验证缓存刷新。二维码只包含公开色卡地址，不包含客户单据地址、登录信息或密钥。

## 5. ERP 内部库存汇总

### 5.1 显示位置与计算方式

在模板 Item 页面增加“颜色库存”区域：各颜色完整货号、色号、辅助颜色名、库存数量及整款合计，支持按仓库查看。

默认仓库建议为手册中的 Finished Goods - SH，实施时核实该仓库仍有效。全仓汇总仅包括当前用户有权查看且属于相应公司的实际仓库，避免重复累计仓库组及其子仓库。

整款账面库存 = 当前范围内所有关联颜色 SKU 的实际库存之和。使用一致的库存单位；不混加不同单位。负库存按真实值计入，不自动归零。停用 SKU 如有剩余库存，内部仍列出并计入，标注停用。

查询时计算汇总，不把总数写为模板的独立库存，不产生额外库存分录。

### 5.2 数字含义

本期展示“账面库存”，不宣称它是可承诺给客户的数量。预留量、在途量与可售量不混入这个合计。

xPos 未同步的离线交易尚未进入服务器记录，因此刷新页面不等于已经包含全部终端交易。页面应说明数量以服务器已入账数据为准；不把页面查询时间冒充所有终端最近同步时间。

内部接口遵循登录用户的公司、物料和仓库权限，不能借模板汇总扩大库存读取范围。公开色卡页面不调用此接口。

## 6. 批发销售单打印选项

### 6.1 两个独立开关

| 开关 | 开启 | 关闭 |
|---|---|---|
| 显示颜色图片 | 商品行显示对应颜色缩略图 | 不留图片空列 |
| 显示色卡二维码 | 打印对应款式的公开色卡二维码 | 不留二维码空白区域 |

开关独立支持四种组合：都关闭、仅图片、仅二维码、两者都开。它们只影响输出，不改变商品、数量、金额、库存或单据状态。

建议首次默认都关闭，以保持紧凑单据；每次打印或导出 PDF 前可选择。客户固定偏好自动记忆属于后续可选增强，首期不要求建立客户偏好配置。

### 6.2 固定内容与版式

无论开关如何选择，均保留完整订货货号、固定色号、商品名称、销售数量，以及现有业务需要的单位、单价、金额等内容。颜色名称辅助显示，不取代固定货号。

建议每个款式打印一个二维码，集中在单据末尾并标款号；同款多色不重复打印相同二维码。开启图片时使用对应颜色照片，避免尺寸过大影响分页。

未发布色卡不输出无效二维码；预览提示该款尚未发布。缺图时显示简短缺图提示，禁止用错误颜色图替代。

浏览器打印与 PDF 导出均需遵守所选开关。普通单据重打可以使用当前图片和当前色卡；若需要保留原始交付版式与图片，应保存当次 PDF。本期不默认建设图片版本归档系统。

### 6.3 单据范围核查

“销售单”尚未在讨论中明确对应 ERPNext 的 Sales Invoice、Sales Order 或 Delivery Note。实施前根据现用的批发单据与打印格式确认主要交付单据，先完成该单据的开单与打印闭环。

建议优先检查 Sales Invoice；不能仅凭名称假定它就是当前业务入口，也不默认同时重做所有单据、POS 小票及标签打印格式。

## 7. 扫码及 xPos 衔接

保留原共用条码挂在模板上的策略；选择器同时显示固定色号、完整订货货号、辅助颜色名和图片。直接输入完整订货货号必须找到唯一具体 SKU。

如客户自行制作颜色条码，可使用完整订货货号编码；本项目只有在明确建立条码或货号识别规则后才保证本系统可扫描，不能假定任何外部系统天然识别。

xPos 离线扫码继续依赖本地镜像。新加的色号、对外货号及相关显示资料如果供桌面使用，须纳入 Hub/Till 同步并验证新增、更新场景；不增加联网查询作为离线扫码兜底。

网页色卡实时读 ERP 资料的行为，与 Till 的离线同步机制分开验收。

## 8. 分阶段实施与交付物

| 阶段 | 操作 | 交付／完成条件 |
|---|---|---|
| 1. 只读核查 | 核对生产版本、真实 app 路径、商品类型、属性、条码、库存、历史单据、批发入口与打印格式 | 每款现状表；明确主要销售单据及数据衔接方式 |
| 2. 整理首款资料 | 选择一款有代表性的窗帘，分色编号、拍照、留样 | SKU／订货号／色号／照片映射表，无重复 |
| 3. 开发数据和界面 | 增加必要字段、发布状态、货号解析、内部汇总及色卡查询 | 在测试环境完成单款闭环 |
| 4. 打印功能 | 接入两个独立开关及所需色号列、图片、二维码 | 四种组合的打印预览和 PDF 样张 |
| 5. xPos 兼容 | 更新必要的显示及本地同步字段 | 联网与离线扫描均落到正确 SKU |
| 6. 商品衔接 | 按批准的逐款映射与盘点结果迁移必要资料和库存 | 变更前后数量、价值与历史引用核对表 |
| 7. 部署与试用 | 按既有 Git／Bench 流程上线，完成内部与游客测试 | 真实批发开单、客户查看色卡、内部查库存通过 |
| 8. 扩展到其余款 | 复用已验证流程批量整理资料 | 全部拟公开款式完成数据校验，更新操作手册 |

已有工作区存在未提交变更，实施时应核对归属并保留；本文档不覆盖现有手册或代码。

## 9. 验收清单

### 9.1 商品和颜色

- [ ] 同款 01、02 唯一，完整订货货号全局唯一，前导零不丢失。
- [ ] 新增颜色不会重排原色号，停用颜色不会释放编号。
- [ ] 实物参考样、后台变体、公开照片、销售单行一一对应。
- [ ] 原条码定位款式，选色后销售及库存记录落在具体 SKU。
- [ ] 已有单据引用和历史货号可追溯。

### 9.2 对外页面

- [ ] 未登录手机可按款号、完整货号查询并放大图片。
- [ ] 共用条码可定位款式；未知货号、未发布款式处理清楚。
- [ ] 页面、公开接口响应和相关公开链接不泄露库存、成本及内部资料。
- [ ] 已停用但曾公开的色号有明确提示，链接不指向其他颜色。
- [ ] 图片更新可见，缺图不错误替代。

### 9.3 内部库存

- [ ] 用已知数量的多个颜色验证分色明细与总数相符。
- [ ] 仓库切换、零库存、负库存、停用有库存 SKU 均正确。
- [ ] 用户看不到无权限仓库的明细或合计。
- [ ] 收货、销售或同步完成后，刷新汇总对应更新。
- [ ] 模板没有新增独立库存，没有重复记账。

### 9.4 打印与业务闭环

- [ ] 图片／二维码四种组合均通过预览和 PDF 验证。
- [ ] 关闭选项后无空列或大块留白，多页单据不裁切内容。
- [ ] 同款多色只打印一次款式二维码，扫码打开正确公开页面。
- [ ] 关闭两个开关仍明确显示完整货号、色号、销售数量和金额。
- [ ] 使用真实岗位权限完成选色、开单、拣货及打印。
- [ ] 原有含税计价、折扣审批、退货规则未受影响。
- [ ] xPos 离线扫码不依赖公开网站；恢复同步后数量一致。

## 10. 部署、数据校验与恢复

所有定制放在 solua_home；如需调整 xPos，限定在其对应应用中。保留 ERPNext/Frappe 核心源码。

按手册记录，本地 WSL 为 v17，生产为 v16；实施前再次核对，关键功能必须在匹配生产版本的环境验证。本地通过不能直接视为生产通过。

部署前备份相关数据库与附件，记录代码版本、商品映射和库存基线。通过 Git 部署，生产应用写入、迁移及构建由 frappe 用户执行，按改动需要 migrate、build、清缓存及重启。

先试点一款，再扩展其余款。库存衔接必须按实际盘点和 ERP 支持的业务凭证执行，保存数量与价值核对结果，不直接修改库存余额表。

代码故障可回退已记录版本并关闭公开入口；已产生正式业务后的数据恢复应使用受控纠正流程，不能简单恢复整库覆盖新交易。新增色号已用于交易后保留其识别关系，不删除以求回退。

交付时记录上线版本、验证证据、打印样张、公开链接，以及仍未补齐的图片或商品资料。

## 11. 实施前需要落实的信息

下列事项不影响已确认的业务方向，在实施只读核查和首款试点阶段解决：

1. 当前批发使用的实际单据类型与打印格式。
2. 每款现有 Item 是普通物料还是模板，是否已有变体、库存与交易。
3. 旧 SKU 采用对外货号映射还是经过评估后重命名。
4. 首款实物颜色清单、照片与参考样；系统不能凭空决定 01 对应哪种实物。
5. 实际可用仓库与库存查看权限。
6. 当前包装条码与内部 SPU、Item 编码的关系；对外“款式货号”采用稳定的客户可识别编码，不误用内部款号。

## 12. 本期之外

不默认包含网上下单、在线支付、客户登录、公开库存、公开价格、客户专属价表、自动记忆客户打印偏好、客户在线标签打印、严格染色批次追踪或全部单据统一改版。

现有手册中的其他待办（期初库存、补图、旧 UAT 清理、同步账号权限收敛等）仍按原任务管理；只有与本次试点直接相关的部分纳入相应阶段，不把历史待办视为本次已完成。
