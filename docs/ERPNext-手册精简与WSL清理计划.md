# ERPNext 手册精简与 WSL 清理计划

日期：2026-09-23
状态：计划与手册重构完成；WSL 清理因安全门未通过而取消。

## 目标

- 安全移除本机已弃用且确认可删除的 ERPNext/Bench 本地环境；保留 WSL 发行版本身。
- 精简《ERPNext 定制开发操作手册.md》，明确区分当前规范与历史记录，保留生产及业务安全规则。
- 只提交本任务相关文件；不部署生产、不推送远端。

## 范围

- 只读检查 WSL 发行版、默认/当前用户、`~/frappe-bench` 及明确关联该 Bench 的专用启动项。
- 修改主手册及确有必要的历史归档、目录入口/交叉引用。
- 验证本地删除结果（若安全门通过）、WSL 启动、Windows 仓库状态与 Markdown 基础质量。

## 禁止事项

- 不连接或操作生产主机 `qq`，不写入生产数据或凭据。
- 不删除 WSL 发行版、`$HOME`、Windows 挂载目录、Docker 全局资源或生产服务器内容。
- 不删除工作区外的用户资料；不删除任何推测出来或未经核实的路径。
- 不删除唯一数据库/站点备份、用户资料、混合用途目录或存在未提交 Git 改动的目录。
- 不部署生产、不推送 GitHub；不提交任何无关工作区改动。

## 现状只读核验

1. `wsl --list --verbose`：仅有 `Ubuntu-24.04`，状态 `Stopped`，WSL 版本 2。
2. `wsl -e sh -lc 'id -un; realpath "$HOME/frappe-bench"; du -sh ...'`：用户 `yang`，HOME `/home/yang`，Bench 规范路径 `/home/yang/frappe-bench`，`du -sh` 为 3.0G。
3. Bench 顶层含 `apps`、`env`、`sites`、`chromium`、`logs`、`config`、Procfile 与 `start.sh`/`setup-erpnext.sh`，不能按单一空壳目录判断。
4. Git 状态：`apps/erpnext` 有 `banking/yarn.lock` 修改；`apps/xpos` 有两份翻译修改及多个新建 DocType 文件、`yarn.lock`。这些均为未提交工作。
5. 唯一性：`sites/dev.localhost/private/backups/` 下有 `20260923_120002-dev_localhost-database.sql.gz`（1,271,220 字节）和对应站点配置备份（244 字节）；站点 `dev.localhost` 存在。未证明有其他副本。
6. WSL 命令输出未发现用户启动配置或系统服务中引用该 Bench 的脚本；该检查不构成删除授权。
7. Windows 主工作区：`C:\Users\Yang\solua-home\sites\erpnext`；手册原始状态干净，另有 12 项既有业务代码修改，全部保留、不纳入提交。

## 备份与可恢复性判断

- 删除前记录目录清单、Git 状态、站点列表和大小；不复制或输出备份内容、配置内容、凭据。
- 有未提交 Bench 改动，且至少有一个站点数据库/配置备份但未证明可恢复或存在第二份。
- 因用户明确规定命中未提交工作或唯一/未验证备份即停止，**本次不删除任何 WSL 路径或自启配置**。WSL 发行版停止状态不等于不能启动，也不做启动测试以免改变机器状态。

## 精确删除目标

- 预期目标仅是 `/home/yang/frappe-bench`；本次安全门失败，实际删除目标为：无。
- 不检查或删除其他用户路径、Windows 挂载、Docker 全局资源、Ubuntu 发行版或生产主机内容。
- 后续如要删除，需先由所有者处理并核实上述未提交改动与唯一备份；之后重新执行完整只读门禁。

## 文档重构映射

- 保留并收拢：生产 `qq` 架构与 `frappe` 用户规则、`solua_home` 不改核心、Git 工作流、备份/恢复、部署/回滚、xPos、商品/仓库/价格/打印及已确认业务规则。
- 删除：WSL2 安装/启动/端口、Redis/MariaDB、Node/Python 本地搭建、本地 v17、WSL Docker 历史与排错，并移除相应目录入口和交叉引用。
- 分区：`当前规范` 与 `历史记录/归档` 清楚分开；主手册只留能指导当前工作的规则，历史细节如不影响安全则归档。
- 过期冲突：Finished Goods 停用、当前仓库树、SH151060 色号 01–10、窗帘库存待办、已关闭交货单的强制开票安排；有仓库证据才改为确定状态，否则标“待生产回读”。
- 安全命令：删除危险通用指令或加安全门，覆盖 `rm -rf apps/solua_home`、`bench update/drop-site/reinstall`、`new-site --force`、移除 scheduler、未经验证的 `fuser -k`。
- 部署流程必须包括备份、精确 commit、差异检查、最小测试、迁移判断、重启、生产回读及回滚点；修正标题编号、重复 4.2、19.21/19.22/19.23 顺序与更新日期。

## 验收点

- 本次 Bench 不删除；报告 WSL 发行版/路径、大小及安全停止证据，不声称完成启动验证。
- 报告手册行数前后、保留/删除章节、冲突修正、危险命令扫描、标题与 Markdown 链接检查结果。
- 用 `rg` 确认主手册不再含 WSL/本地 v17/本地 `frappe-bench` 安装指导；生产服务器路径必须明确标为 `qq` 生产。
- 检查 `git diff --check` 与 `git status`；提交范围仅包含本计划、手册和本任务新增的必要文档。

## Git 提交策略

- 主工作区既有 12 项业务文件修改保持不变；工作树 checkout 中另有大量既有差异，均不带入本任务。
- 只对本任务文件选择性暂存；暂存后复核 staged diff 与路径清单。
- 有任何无法区分的预存改动或仓库状态异常时不提交；不推送 GitHub。

## 失败停止条件

- WSL 不可用、命令无法证明当前用户/路径、实际目标不精确匹配、含唯一数据/用户资料、Git 有未提交改动、发现混合内容，立即停止删除。
- 生产相关事实无法由当前仓库证据确认，标记“待生产回读”，不猜测、不操作 qq。
- Windows 工作区状态异常、手册当前差异无法归属、Markdown 链接/锚点无法可靠检查，停止相关编辑或提交并列出待处理证据。


## 执行结果与验收记录（2026-09-23）

- **WSL 清理**：删除未执行。唯一候选为 `/home/yang/frappe-bench`（3.0G）；发行版 `Ubuntu-24.04` 仍登记为 WSL 2，核验时状态为 `Stopped`。发现未提交的 `apps/erpnext/banking/yarn.lock` 修改，以及 `apps/xpos` 的两份翻译修改、多个未跟踪 DocType 文件和 `yarn.lock`；`dev.localhost` 站点内找到数据库备份和站点配置备份，未证明存在第二份或可恢复。因此不删除任何目录/服务，不启动 WSL；没有删除后大小或启动验证结果。
- **手册**：完整读取后从 3,618 行 / 198,226 字节精简为 165 行 / 13,487 字节。移除本地开发环境搭建、依赖/端口/Redis/MariaDB/Docker 排错、重复命令速查以及逐次部署/会话流水；保留生产 `qq` 架构、用户权限、定制边界、Git、备份/恢复/发布/回滚、xPos、商品/仓库/价格/打印规则。
- **冲突修正**：记录内最近仓库回读（2026-09-18）称 `Finished Goods - SH` 已停用、`Receiving - SH` 为默认收货仓并列出现存树；注明该信息未在本任务实时回读，且本地默认值仍含旧仓，操作前待生产回读。窗帘库存已按既有生产记录完成，不再列为待办。SH151060 本地色卡文件为 01–10；生产 Item/Variant 回读未执行，明确标待确认。已关闭交货单不要求补填开票安排或重新打开。
- **危险命令扫描**：主手册不含递归删除 App、`bench update/drop-site/reinstall`、`new-site --force`、`fuser -k` 或移除 scheduler 的可执行步骤；安全边界要求精确目标、备份和可验证回滚。
- **Markdown 检查**：主手册 13 个链接均可解析、标题锚点有效；3 份 Markdown 的代码围栏成对；`git diff --check` 通过。
- **Git 边界**：提交前只暂存本任务 3 个 Markdown 文件；提交后本任务文件均干净，工作区仍有 12 项跟踪文件修改和 615 个无关未跟踪文件；不推送。
